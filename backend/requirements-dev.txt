# Dependências de DESENVOLVIMENTO e CI. Não entram na imagem de produção —
# `backend/Dockerfile` instala apenas o requirements.txt.
#
# Instalar:
#     pip install -r backend/requirements-dev.txt

pytest==8.3.3
ruff==0.6.9
